<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeans.cosmetic_project.login.dao.loginDao">

  <select id="login" parameterType="com.jeans.cosmetic_project.login.dto.loginRequestDto">
    SELECT
        seq as seq,
        id as id,
        name as name,
        age as age,
        email as email,
        phone as phone,
        birthday as birthday
    FROM
        users
    WHERE
        id = #{id}
    AND
        password = #{password}
  </select>
</mapper>